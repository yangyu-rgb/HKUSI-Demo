# 已退役：真实分钟模型就绪流程

本项目已确定只用于 SIUS2612 课堂 Demo，不开展现场真实等待数据收集，也不进行生产模型晋级。

原有 `/api/demo/v2-readiness`、200条真实授权标签门槛、真实标签训练快照和生产晋级面板已经退出公开产品流程。众包记录只用于当前课堂预测校准，不进入训练数据。

当前课堂就绪标准由 `GET /api/demo/v1-readiness` 和模型页共同展示：双向路线矩阵、本地 Provider、SQLite、Demo 身份、AI v1 影子产物、AI v2.2 主产物和本地通知适配器均可用。

香港与深圳公开数据均以仓库快照形式服务演示；等待分钟仍为可解释生成目标，任何离线指标都不得解释为真实口岸准确率。
