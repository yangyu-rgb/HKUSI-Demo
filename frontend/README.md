# Frontend

React + TypeScript + Vite dashboard for the Topic 2 local demo.

## Run

```bash
npm install
npm run dev
```

The frontend expects the backend at:

```text
http://127.0.0.1:8000
```

Override with:

```bash
VITE_API_BASE=http://127.0.0.1:8000 npm run dev
```

